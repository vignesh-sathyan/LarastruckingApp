CREATE TABLE [dbo].[tblAccidentDocument] (
    [AccidentDocumentId] INT          IDENTITY (1, 1) NOT NULL,
    [Name]               VARCHAR (50) NULL,
    PRIMARY KEY CLUSTERED ([AccidentDocumentId] ASC)
);

